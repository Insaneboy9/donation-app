{
  "name": "donation_blockchain_client",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "npm run build && node build/main.js",
    "write": "npm run build && node build/writeDummy.js",
    "start:dev": "npm run env && npx nodemon",
    "build": "rimraf ./build && tsc"
  },
  "keywords": [],
  "author": "",
  "dependencies": {
    "firebase-admin": "^11.5.0",
    "web3": "^1.8.1"
  },
  "devDependencies": {
    "@types/node": "^18.11.18",
    "nodemon": "^2.0.20",
    "rimraf": "^4.1.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  }
}
